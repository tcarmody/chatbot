# Anthropic API Key
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_api_key_here

# Application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Vercel Postgres Database
# For local development, create a Vercel Postgres database and copy the connection string
# These are automatically set when you link a Vercel Postgres database to your project
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# HubSpot Configuration
# Create a private app in HubSpot: Settings > Integrations > Private Apps
# Required scopes: tickets (read/write)
HUBSPOT_ACCESS_TOKEN=your_hubspot_access_token_here

# HubSpot Pipeline Configuration (optional)
# Get these IDs from: HubSpot Settings > Objects > Tickets > Pipelines
# Click on a pipeline stage to see its internal ID in the URL
# If not set, defaults will be used (may not match your HubSpot setup)
HUBSPOT_PIPELINE_ID=
HUBSPOT_STAGE_OPEN=1
HUBSPOT_STAGE_IN_PROGRESS=2
HUBSPOT_STAGE_RESOLVED=3
HUBSPOT_STAGE_CLOSED=4

# Sentry Error Tracking (optional)
# Create a project at https://sentry.io and get your DSN
# Leave empty to disable error tracking
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_DSN=
